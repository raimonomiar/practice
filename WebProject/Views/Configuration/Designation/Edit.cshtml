@model DesignationModel
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-md-4">
    <div class="box">
        <div class="box-header with-border">
            <h3 class="box-title">Edit Designation</h3>
            @*<span class="pull-right">
                    <a class="btn btn-primary" href="@Url.Action("Create","Designation")"><i class="fa fa-plus"></i> Add New</a>
                </span>*@
        </div>
        <!-- /.box-header -->
        <div class="box-body">

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-group">
                    @Html.LabelFor(x =>x.Code , new { @class = "control-label" })

                    @Html.TextBoxFor(x => x.Code, new { @class = "form-control" })

                    @Html.ValidationMessageFor(x => x.Code, null, new { @class = "text-red" })
                </div>

                <div class="form-group">
                    @Html.LabelFor(x => x.DesgName, new { @class = "control-label" })

                    @Html.TextBoxFor(x => x.DesgName, new { @class = "form-control" })

                    @Html.ValidationMessageFor(x => x.DesgName, null, new { @class = "text-red" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(x => x.DiplayOrder, new { @class = "control-label" })

                    @Html.EditorFor(x => x.DiplayOrder, new { htmlAttributes = new { @class = "form-control" } })

                    @Html.ValidationMessageFor(x => x.DesgName, null, new { @class = "text-red" })
                </div>
                <div class="box-footer">
                    <a href="@Url.Action("Index","Designation")" class="btn btn-default">Cancel</a>
                    <button type="submit" class="btn btn-info pull-right click">Update</button>
                </div>
            }
        </div>
        <!-- /.box-body -->

    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
