@model EducationLevelModel
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-md-4">
    <div class="box">
        <div class="box-header with-border">
            <h3 class="box-title">Add Education Level</h3>
            @*<span class="pull-right">
                    <a class="btn btn-primary" href="@Url.Action("Create","Colour")"><i class="fa fa-plus"></i> Add New</a>
                </span>*@
        </div>
        <!-- /.box-header -->
        <div class="box-body">

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-group">
                    @Html.LabelFor(x => x.LevelName, new { @class = "control-label" })

                    @Html.TextBoxFor(x => x.LevelName, new { @class = "form-control" })

                    @Html.ValidationMessageFor(x => x.LevelName, null, new { @class = "text-red" })
                </div>

                <div class="form-group">
                    @Html.LabelFor(x => x.DisplayOrder, new { @class = "control-label" })

                    @Html.EditorFor(x => x.DisplayOrder, new { htmlAttributes = new { @class = "form-control" } })

                    @Html.ValidationMessageFor(x => x.DisplayOrder, null, new { @class = "text-red" })
                </div>

                <div class="box-footer">
                    <a href="@Url.Action("Index","Religion")" class="btn btn-default">Cancel</a>
                    <button type="submit" class="btn btn-info pull-right click">Create</button>
                </div>
            }
        </div>
        <!-- /.box-body -->

    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}




